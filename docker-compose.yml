services:
  notebook:
    build:
      context: ./notebook
      dockerfile: Dockerfile
    container_name: mi-jupyter-dev
    ports:
      - "8888:8888"
    volumes:
  
      - ./:/app
    restart: unless-stopped